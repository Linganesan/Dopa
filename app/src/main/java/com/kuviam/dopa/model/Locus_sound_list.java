package com.kuviam.dopa.model;

import com.kuviam.dopa.model.DaoSession;
import de.greenrobot.dao.DaoException;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT. Enable "keep" sections if you want to edit. 
/**
 * Entity mapped to table "LOCUS_SOUND_LIST".
 */
public class Locus_sound_list {

    private Long id;
    private long locusId;
    /** Not-null value. */
    private byte[] item;

    /** Used to resolve relations */
    private transient DaoSession daoSession;

    /** Used for active entity operations. */
    private transient Locus_sound_listDao myDao;

    private Locus locus;
    private Long locus__resolvedKey;


    public Locus_sound_list() {
    }

    public Locus_sound_list(Long id) {
        this.id = id;
    }

    public Locus_sound_list(Long id, long locusId, byte[] item) {
        this.id = id;
        this.locusId = locusId;
        this.item = item;
    }

    /** called by internal mechanisms, do not call yourself. */
    public void __setDaoSession(DaoSession daoSession) {
        this.daoSession = daoSession;
        myDao = daoSession != null ? daoSession.getLocus_sound_listDao() : null;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public long getLocusId() {
        return locusId;
    }

    public void setLocusId(long locusId) {
        this.locusId = locusId;
    }

    /** Not-null value. */
    public byte[] getItem() {
        return item;
    }

    /** Not-null value; ensure this value is available before it is saved to the database. */
    public void setItem(byte[] item) {
        this.item = item;
    }

    /** To-one relationship, resolved on first access. */
    public Locus getLocus() {
        long __key = this.locusId;
        if (locus__resolvedKey == null || !locus__resolvedKey.equals(__key)) {
            if (daoSession == null) {
                throw new DaoException("Entity is detached from DAO context");
            }
            LocusDao targetDao = daoSession.getLocusDao();
            Locus locusNew = targetDao.load(__key);
            synchronized (this) {
                locus = locusNew;
            	locus__resolvedKey = __key;
            }
        }
        return locus;
    }

    public void setLocus(Locus locus) {
        if (locus == null) {
            throw new DaoException("To-one property 'locusId' has not-null constraint; cannot set to-one to null");
        }
        synchronized (this) {
            this.locus = locus;
            locusId = locus.getId();
            locus__resolvedKey = locusId;
        }
    }

    /** Convenient call for {@link AbstractDao#delete(Object)}. Entity must attached to an entity context. */
    public void delete() {
        if (myDao == null) {
            throw new DaoException("Entity is detached from DAO context");
        }    
        myDao.delete(this);
    }

    /** Convenient call for {@link AbstractDao#update(Object)}. Entity must attached to an entity context. */
    public void update() {
        if (myDao == null) {
            throw new DaoException("Entity is detached from DAO context");
        }    
        myDao.update(this);
    }

    /** Convenient call for {@link AbstractDao#refresh(Object)}. Entity must attached to an entity context. */
    public void refresh() {
        if (myDao == null) {
            throw new DaoException("Entity is detached from DAO context");
        }    
        myDao.refresh(this);
    }

}
